#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/sh" ]; then
  "$basedir/sh"  "$basedir/../np/np.sh" "$@"
  ret=$?
else 
  sh  "$basedir/../np/np.sh" "$@"
  ret=$?
fi
exit $ret
